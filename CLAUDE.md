# tripwire

API drift watcher. Live at tripwire.heyitsmejosh.com. Came out of a YC RFS pick via sparkjar.

- `web/` is the site and Worker, `tui/` the SwiftPM terminal client (thin fetch against the live API)
- The README is the house writing reference. Do not loosen it
- Deploy with `wrangler` from `web/`
